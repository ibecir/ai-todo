#name: Build and Deploy Docker Image
#
#on:
#  push:
#    branches:
#      - main
#
#jobs:
#  build-and-deploy:
#    runs-on: ubuntu-latest
#
#    steps:
#      - name: Checkout code
#        uses: actions/checkout@v3
#
#      - name: Login to DockerHub
#        uses: docker/login-action@v2
#        with:
#          username: ${{ secrets.DOCKER_USERNAME }}
#          password: ${{ secrets.DOCKER_PASSWORD }}
#
#      - name: Build and push Docker image
#        run: |
#          docker build -t your-dockerhub-username/spring-api:latest .
#          docker push your-dockerhub-username/spring-api:latest
#
#      - name: Deploy Docker container on DigitalOcean
#        uses: appleboy/ssh-action@v0.1.5
#        with:
#          host: ${{ secrets.DO_SSH_HOST }}
#          username: ${{ secrets.DO_SSH_USER }}
#          key: ${{ secrets.DO_SSH_KEY }}
#          script: |
#            docker pull your-dockerhub-username/spring-api:latest
#            docker stop spring-api || true
#            docker rm spring-api || true
#            docker run -d --name spring-api -p 8080:8080 your-dockerhub-username/spring-api:latest
